@foreach (var usg in Model.Usings)
{
@:using @usg;
}

namespace @Model.Namespace;

public class @Model.EntityName : IBaseEntity<@Model.KeyType>
{
    @for (int i = 0; i < Model.Properties.Count; i++)
    {
        var property = Model.Properties[i];
        if (property.IsCollectionNavigation)
        {
    @:public ICollection<@property.Type> @property.Name { get; set; }
        }
        else
        {
    @:public @property.Type @property.Name { get; set; }
        }
        if (i != Model.Properties.Count - 1)
        {
    @:
        }
    }
}